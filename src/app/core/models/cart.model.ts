/** TODO: fill in from the backend `CartSerializer` once submitted. */
export interface Cart {
  id: number;
  items: CartItem[];
}

/** TODO: fill in from the backend `CartItemSerializer` once submitted. */
export interface CartItem {
  id: number;
}
