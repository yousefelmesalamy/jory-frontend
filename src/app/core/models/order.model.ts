/** TODO: fill in from the backend `OrderSerializer` once submitted. */
export interface Order {
  id: number;
  items: OrderItem[];
}

/** TODO: fill in from the backend `OrderItemSerializer` once submitted. */
export interface OrderItem {
  id: number;
}
