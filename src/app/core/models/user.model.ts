/** TODO: fill in from the backend `UserSerializer` once submitted. */
export interface User {
  id: number;
  email: string;
}

/** TODO: fill in from the backend `AddressSerializer` once submitted. */
export interface Address {
  id: number;
}
